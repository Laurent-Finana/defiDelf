{% extends 'admin/admin_base.html.twig' %}

{% block title %}Modifier un partenaire{% endblock %}

{% block body %}
<main>
    <div class="container mt-5 pt-5">
        <h1 class="mt-5 pt-5 text-center fw-bold">Modifier un partenaire</h1>

        {{ include('admin/partner/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('app_admin_partner_index') }}" class="btn btn-warning my-2">Retour à la liste</a>

        {{ include('admin/partner/_delete_form.html.twig') }}
    </div>
</main>
{% endblock %}
