{% extends 'admin/admin_base.html.twig' %}

{% block title %}Partenaire{% endblock %}

{% block body %}
<main>
    <div class="container my-5 pt-5">
        <h1 class="mt-5 pt-5 fw-bold text-center">Partenaire</h1>

        <img src="{{ vich_uploader_asset(partner, 'thumbnailFile') }}" class="d-block mx-auto" width="500" alt="Logo du partenaire">

        <table class="table">
            <tbody>
                <tr>
                    <th>Nom</th>
                    <td>{{ partner.name }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_admin_partner_index') }}" class="btn btn-warning mb-1">Retour à la liste</a>

        <a href="{{ path('app_admin_partner_edit', {'id': partner.id}) }}" class="btn btn-primary mb-1">Modifier</a>

        {{ include('admin/partner/_delete_form.html.twig') }}
    </div>
</main>
{% endblock %}
