{% extends 'admin/admin_base.html.twig' %}

{% block title %}Planning{% endblock %}

{% block body %}
<main>
    <div class="container mt-5 pt-5">
        <h1 class="mt-5 mb-4 pt-5 text-center fw-bold">Dates</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Delf A1</th>
                <td>{{ planning.DelfA1 }}</td>
            </tr>
            <tr>
                <th>Delf A2</th>
                <td>{{ planning.DelfA2 }}</td>
            </tr>
            <tr>
                <th>Delf B1</th>
                <td>{{ planning.DelfB1 }}</td>
            </tr>
            <tr>
                <th>Delf B2</th>
                <td>{{ planning.DelfB2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_planning_index') }}" class="btn btn-warning mb-2">Retour à la liste</a>

    <a href="{{ path('app_admin_planning_edit', {'id': planning.id}) }}" class="btn btn-primary mb-2">Mettre à jour</a>

    {{ include('admin/planning/_delete_form.html.twig') }}
    </div>
</main>
{% endblock %}
