{% extends 'base.html.twig' %}

{% block title %}Modification de l'utilisateur{% endblock %}

{% block body %}
<main>
    <div class="container mt-5 mb-4 pt-5">
        <h1 class="mt-5 mb-4 pt-5 text-center fw-bold">Utilisateur : {{ user.lastname }} {{ user.firstname }}</h1>

        {{ include('admin/user/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('app_admin_user_index') }}" class="btn btn-warning my-1">Retour liste</a>

        {{ include('admin/user/_delete_form.html.twig') }}
    </div>
</main>
{% endblock %}
